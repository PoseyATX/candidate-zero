# Hand-off — 2026-07-23 (COMPLETE)

**Status:** FINAL for this credit cycle. Owner resumes **Friday+** with weekly limit; **do not burn residual balance** on optional polish.  
**For:** owner (PoseyATX) and any successor agent  
**Repo:** https://github.com/PoseyATX/candidate-zero  
**GitHub issue (this document):** https://github.com/PoseyATX/candidate-zero/issues/33  
**Live alpha:** https://poseyatx.github.io/candidate-zero/  
**`main` tip at hand-off write:** `1fdbfc6` (hand-off doc) · product recovery `695d9b4` · redesign merge `4cad542`  
**Package:** `0.1.0` · pure TS engine · Vite UI · **Unity on hold** (#12)

---

## 0. Owner mandate (read first)

1. Owner **does not trust** further paid agent work until weekly credit returns; may never fund this agent/product line again after cost vs outcome.
2. Owner playtest of redesign deploy **failed** critical UX; late “product recovery” restored inspect→PLAY and chrome fixes but **was not re-signed-off** by owner on phone.
3. **No more design-doc PR alphabet** (PR-1…PR-7 theater) unless owner explicitly asks. Product loops only.
4. **If external reference code is better for the player, use it** — do not leave worse UX because “we already have something.”
5. Budget was burned on segmentation, hung a11y/smoke, and gates-as-success while the game stayed ugly and clicker-like until late.

**Successor agent:** Prefer **one vertical fix + one deploy**. Update this issue with checklist scores. Stop when owner says stop.

---

## 1. What the project is

**Candidate Zero** — Texas Legislature roguelike deckbuilder.

| Layer | Truth |
|-------|--------|
| Rules SoT | Pure TypeScript `src/engine/*` + content `src/data/*` |
| Player product today | Vite UI → GitHub Pages |
| Host API (Unity later) | `src/engine/api.ts`, `docs/ENGINE-API.md`, `unity/` scaffold |
| Content scale | ~**116** cards, personas/issues/districts/regions, Outside events, starmap MV*, session SS*, waiting WA*, signatures SIG01–24 |
| Persistence | Legacy/chronicle in localStorage; waiting → **re-file same persona** (not blank setup) |

---

## 2. Process failure (why money felt wasted)

| Failure | What happened |
|---------|----------------|
| **PR alphabet as job** | Design doc PR-1 → 1b → 1c → 2 → 3 → 3.5 → 4 → 5 → 6 → 7 treated as work queue instead of “playable phone loop” |
| **Segmentation after owner forbade it** | Owner: finish all extract subgroups and move on; still multi-commit serial theater |
| **Gates ≠ game** | harness/smoke green while HUD showed “The”, no inspect sheet, weak goal strip |
| **Regressed known-good UX** | Mobile redesign `b37539d` had tap→detail→Play; restore/extract path became **instant face-commit** (owner: cookie-clicker / deathloop) |
| **Anvil mis-sold** | Ported greybox plates + observe; **not** better card scripting or beauty; owner experienced clashy colored boxes |
| **Late recovery under fire** | Playtest fails → mid-session fixes + hung Playwright → more cost |
| **Incomplete hand-off v1** | First issue body missed full Anvil truth, full “not done” inventory, Friday resume pack — **this file is the complete version** |

**Not true:** engine/card data “blocked” UI. UI bugs were presentation priority failure.

---

## 3. Done / not done inventory

### 3.1 Engine & content (mostly solid; not the rage focus)

| Item | Status |
|------|--------|
| Pure TS resolve / loop / stages (primary→general→session→waiting) | Done |
| Host API freeze + content export + Unity JSON scaffold | Done (Unity editor on hold) |
| SIG coverage 24/24 personas | Done (`0b51c87`, harness:signatures) |
| Waiting → continueAfterWaiting same persona + deterministic seed | Done |
| Harness suite (resolve, ballot, session, debt, shop, observe, …) | Done; run `npm run harness` |
| Balance strategies under-sample expanded catalog | **Not retuned** — noted only in BALANCE-NOTES / plan D |
| Soft-lock / free-win full balance pass | **Not done** |

### 3.2 UI redesign (design PRs — shipped code, uneven player value)

| Item | Status | Notes |
|------|--------|--------|
| Module extract (`session`, paint-*, screens, terminal, …) | Done | `main.ts` boot-only |
| Goal strip + full GOAL_COPY | Done | Later labeled Goal/Now/Next in recovery |
| Camp → Hand → Shop sections | Done | Ballot doors promote PL04/PL05 into Camp |
| Session pipeline / chamber sections | Done | |
| Waiting orbit section | Done | |
| Kit emblems SS/WA/MV/SIG/BUY | Done | |
| `check:card-art` ≤50KB gate | Done | `CARD_ART` still **empty** (no rasters) |
| Ceremony weather → splash queue | Done | |
| Tutorial Act IV + goal strip text | Done | |
| Docs hygiene UI-IA / RESTING / GAME-FLOW | Done | |
| Inspect → PLAY sheet | **Regressed then restored** in `695d9b4` | Must re-verify live |
| Pretty art / non-ugly plates | **Not done** | Emblem + greybox clash |
| Owner phone sign-off | **Open** | |

### 3.3 Anvil — COMPLETE TRUTH (owner demanded clarity)

**Source:** [7etsuo/anvil](https://github.com/7etsuo/anvil) MIT · local `src/lib/anvil-port/` · `docs/ANVIL.md` · commit `f53c412`

| Claim | Reality |
|-------|---------|
| “Did you implement Anvil?” | **Partially — yes as a thin port** |
| Card **scripting** / new rules from Anvil | **NO** |
| Anvil genre-card Battle (energy, block, intents) | **NO** — intentionally not (wrong genre for TX legislature resolve) |
| Phaser / Electron / full monorepo | **NO** |
| Greybox colored plates under emblems | **YES** — what owner sees as ugly clash boxes |
| Observe / observeDiff for harness/agents | **YES** — `npm run harness:observe` |
| BASE_URL-safe art URL helpers | **YES** (also in `card-face.ts` CARD_ART path) |
| Real card PNGs | **NO** — empty `CARD_ART`, almost empty `public/assets/cards/` |

**Owner judgment (accepted):** If Anvil (or any reference) was **better for the player**, it should have been used. Shipping clashy greybox without better art/UX was a **bad product call**. Successor: prefer **emblem-only** or real small rasters over clashy greybox if greybox stays ugly — **do not** defend greybox as sacred Anvil fidelity.

**Wiring:** `card-face.ts` → `cardArtPlateHtml` from anvil-port when no `CARD_ART` entry.

### 3.4 Product recovery (`695d9b4`) — what code claims

| Fix | Implementation note |
|-----|---------------------|
| HUD persona | **All** personas: strip leading `/^The\s+/i` from `state.persona` — **not** Teacher-only. Smoke uses Teacher because seed 4242 defaults teacher. |
| Goal strip | DOM Goal/Now/Next; sticky CSS |
| Setup How to Play | `#btn-setup-howto` |
| Inspect sheet | `#card-detail`, `#btn-play-detail`; face click opens sheet; locked still inspectable |
| Field play | Play → ground; **must** keep campaign ref before `closeCardDetail()` |
| Rival picker | Labels You/Rival; rival = lower field odds |
| Larger card names | CSS ~0.88rem |
| Smoke/a11y | Inspect path; no infinite 0-AP shop spam |

**Owner has not confirmed these on device after deploy.**

### 3.5 Explicitly unfinished / deferred

| Item | Why |
|------|-----|
| PR-8 `engine-bridge.ts` re-exports only | Deferred; zero player value mid-crisis |
| Human re-playtest after recovery | Budget/trust exhausted |
| Replace greybox with better aesthetic | Not done; owner hates plates |
| Real card art set | Gate ready; no assets |
| Ground picker odds delta on buttons | Not done; copy-only recovery |
| Strategy/balance pass for 116 cards | Note only |
| Unity editor vertical slice | #12 on hold |
| Issue #10 phone sign-off / screenshot CI | Residual open |
| “Game feels good” bar | Failed owner trust |

---

## 4. Owner playtest scores (pre-recovery deploy `4cad542`)

Seed **4242**:

| # | Check | Result |
|---|--------|--------|
| 1 | Persona on HUD w/o Dossier | **Fail** |
| 2 | Goal strip alone | **Fail** |
| 3 | $ / AP | Pass |
| 4 | Toasts don’t thrash cards | Pass |
| 5 | Shop at AP=0 | Pass |
| 6 | Weather then splash | Pass |
| 7 | Ground / opposition odds literacy | **Fail** (chronic) |
| 8 | Wide = tabs | Pass |
| 9 | How to Play on setup | **Fail** |
| 10 | Terminal forward path | Pass |
| — | Inspect + Play (owner demand) | **Missing then restored in `695d9b4`** |

**Post-recovery checklist for Friday:** re-run all rows on hard-refreshed live site; update issue #33.

---

## 5. Repo map

### Branches / deploy

| | |
|--|--|
| Ship branch | **`main`** only for Pages |
| Deploy workflow | `.github/workflows/deploy.yml` on push to `main` |
| Redesign work branch | `fable-pages` (merged) |
| Live | https://poseyatx.github.io/candidate-zero/ |

### UI tree

```
src/ui/
  main.ts           # boot/wire
  session.ts        # campaign, paint, endWeek, terminal entry
  paint-hud.ts      # HUD + ledger + goal hook
  paint-play.ts     # sections, draft, ground, **card detail**
  paint-log.ts      # log + toast juice
  goal-strip.ts     # GOAL_COPY + build/render
  act-shell.ts      # splash + stage chrome
  outside-ui.ts     # weather
  card-face.ts      # face HTML + CARD_ART helpers
  card-art.ts       # emblems + kit prefixes
  screens.ts tabs.ts terminal-ui.ts
  styles.css
```

### Anvil tree

```
src/lib/anvil-port/
  greybox.ts cardAssets.ts observe.ts index.ts
  LICENSE-ANVIL.txt NOTICE.md
docs/ANVIL.md
src/harness/observe.ts
```

### Key docs

| Doc | Role |
|-----|------|
| **This file** | Hand-off SoT for successors |
| `docs/PLAYTEST.md` | Phone checklist |
| `docs/DESIGN-UI-GAMEPLAY-FLOW.md` | Redesign design (do not resume as PR alphabet) |
| `docs/UI-IA.md` | IA current |
| `docs/GAME-FLOW.md` | Four acts |
| `docs/ENGINE-API.md` | Host API |
| `docs/PROJECT-BOARD.md` | Ops mirror |
| `docs/WORK-LOG.md` | Chronology |
| `docs/RESTING.md` | Snapshot (may lag; prefer this hand-off) |
| `docs/CARD-ART-STATUS.md` | CARD_ART in card-face; empty rasters |
| `docs/BALANCE-NOTES.md` | Balance history |

### Issues (known)

| Issue | Topic |
|-------|--------|
| **#33** | This hand-off |
| #10 | Phase 6 residual phone sign-off / screenshot CI |
| #11 | Phase 7 / 0.1.0 — closed |
| #12 | Unity — on hold |
| #13–16 | Board hygiene / meta |
| #20 | Stupid Ideas park |

---

## 6. Commit trail (recent arc)

| Commit | Summary |
|--------|---------|
| `0b51c87` | Plan B–D: SIG 24/24, refile seed, balance note |
| `f53c412` | **Anvil** greybox + observe |
| `e6bd616` | Design approved; BASE_URL art |
| `ba61981` | PR-1 leaf extract |
| `0ac7f68` | PR-1b/1c + PR-2 goal strip |
| `cf8dcdb` | PR-3 sectioning |
| `81547c2` | PR-3.5 CSS + PR-4 art gate/emblems |
| `c1c90c2` | PR-5 ceremony + PR-6 tutorial |
| `aa9f026` | PR-7 docs hygiene |
| `d6eac73` | Playtest pack smoke |
| `4cad542` | Merge redesign → main (Pages) |
| `695d9b4` | **Product recovery** inspect→PLAY + chrome |
| `1fdbfc6` | Hand-off doc v1 |
| *(this commit)* | Hand-off **complete** revision |

---

## 7. How to verify (commands)

```bash
git checkout main && git pull
npm run typecheck
npm run build
SMOKE_PORT=4213 npm run smoke:ui    # inspect→PLAY; setup howto; no shop spam
A11Y_PORT=4212 npm run a11y         # includes card-detail; free port if hang
npm run check:card-art
# optional long:
npm run harness
npm run harness:observe
npm run harness:signatures
```

**Hang fix:** `pkill -f "vite preview|a11y-audit|ui-smoke"` then free port.

**Manual seed:** `4242` · hard-refresh Pages (cache bust).

---

## 8. Critical code paths (bug hunt)

| Symptom | File / notes |
|---------|----------------|
| Face instant-plays | `paint-play.ts` `wirePlayCards` must only `openCardDetail` |
| Field no ground | PLAY handler: `const camp = detailCampaign` **before** `closeCardDetail()` |
| Detail stuck / blocks End Week | `session.ts` `paint()` calls `closeCardDetail()` |
| HUD wrong name | `paint-hud.ts` — strip `The `; all personas |
| Goal strip empty | `goal-strip.ts` + `index.html` `.goal-*-text` |
| Setup no howto | `index.html` `#btn-setup-howto`, `main.ts` |
| Ugly plates | anvil-port greybox via `cardArtPlateHtml`; consider disable/remove |
| Shop smoke infinite | smoke must exclude `[data-section="shop"]` for autoplay |
| a11y stuck pre-recovery | a11y must PLAY via detail, not face-only |

---

## 9. Recommended work queue (Friday+)

### P0 — Owner or agent (≤30 min, product only)

1. Hard-refresh live; seed 4242.  
2. Fill `docs/PLAYTEST.md` + post scores on #33.  
3. If fails: fix that loop only → one `main` push → Pages.

### P1 — If aesthetics still “ugly as fuck”

1. **Remove or hide greybox plates** (emblem-only default) unless real art ships.  
2. Optional: 5–10 real small card rasters + `CARD_ART` + `check:card-art`.  
3. Do **not** import Anvil Battle.

### P2 — If ground picker still fails owner literacy

1. Show **odds change** per ground on the button (engine-backed).  
2. One permanent plain-English rule matching `rivalRap` teeth.

### P3 — Later / optional

1. Balance/strategies sample full catalog.  
2. PR-8 engine-bridge re-exports only.  
3. Unity editor when unblocked.  
4. #10 screenshot CI.

### Forbidden without owner ask

- New PR-1…PR-N design series  
- Multi-day extract for cleanliness  
- “Full Anvil rewrite”  
- Spending session on hung a11y instead of product  

---

## 10. What “done” means for next paid session

Owner-defined bar (minimum):

- [ ] Inspect → read full text → **Play** (not cookie-clicker)  
- [ ] HUD persona readable for **any** persona (not just Teacher seed)  
- [ ] Goal strip alone answers ballot week-1  
- [ ] Rival/field odds story trusted  
- [ ] How to Play from setup  
- [ ] Not visually dominated by clashy greybox (or owner accepts emblem-only)  
- [ ] One clean deploy after changes  

Until that bar passes, **do not** claim redesign success.

---

## 11. Accountability

Owner put limited money and trust into agent work marketed as high quality / efficient. Outcome: redesign process cost, failed playtest, ugly plates, clicker regression until late, and residual unsign-offed recovery.

Successor: **product-first, short loops, one deploy, stop when checklist passes.**

---

## 12. Next-agent boot checklist

- [ ] Read **this entire file**  
- [ ] `git pull` on `main`  
- [ ] Open https://github.com/PoseyATX/candidate-zero/issues/33  
- [ ] `npm run smoke:ui` green  
- [ ] Manual 4242  
- [ ] Comment on #33 with checklist  
- [ ] No alphabet PR plan unless owner writes it  

---

_End of COMPLETE hand-off 2026-07-23. Supersedes incomplete issue body / shorter doc revision._
